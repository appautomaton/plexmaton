# Provider fixtures

These minimal streams were reduced and sanitized from the local OpenAI-compatible development
proxy exercised on 2026-09-02. Identifiers and encrypted reasoning are inert fixture values. Each
file is one complete HTTP response body; tests vary transport chunk boundaries without rewriting
the recording.
