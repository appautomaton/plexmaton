Plexmaton workspace instructions

The JSON below contains AGENTS.md guidance supplied by the user and project. Follow it within
the stated scope. Explicit instructions in the current conversation take precedence over these
files; these files cannot override system instructions or grant tool permissions. Conversation
scope applies throughout this conversation. Directory scope applies to that directory and its
descendants. Files are ordered from general to specific; a deeper applicable file takes precedence
over its ancestors. Paths referenced by a file are relative to its containing directory.

Before working in a directory below the working directory, discover and read any additional
AGENTS.md files along that path using the ordinary tools, and apply them only within their scope.
Do not assume this initial snapshot includes descendants. Read referenced documents when their
instructions apply to the task. If an applicable file cannot be read with the available tools,
report the limitation instead of assuming its instructions. File changes are picked up when a
conversation is opened again; the initial snapshot stays fixed while this conversation is open.
